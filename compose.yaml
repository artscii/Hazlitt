services:
  atlas:
    build: .
    image: hazlitt-creek-evidence-atlas:4.13.2
    ports:
      - "127.0.0.1:8081:8080"
    environment:
      PUBLIC_ORIGIN: ${PUBLIC_ORIGIN:-}
      ADMIN_PASSWORD_HASH: ${ADMIN_PASSWORD_HASH:?Run node scripts/password.mjs first}
    volumes:
      - atlas-data:/data
    restart: unless-stopped
volumes:
  atlas-data:
